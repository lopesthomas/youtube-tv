<assembly xmlns="http://maven.apache.org/plugins/maven-assembly-plugin/assembly/1.1.3"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/plugins/maven-assembly-plugin/assembly/1.1.3 https://maven.apache.org/xsd/assembly-1.1.3.xsd">
  <id>dist</id>
  <formats>
    <format>zip</format>
  </formats>
  <includeBaseDirectory>true</includeBaseDirectory>
  <baseDirectory>${project.artifactId}-${project.version}</baseDirectory>

  <files>
    <file>
      <source>${project.build.directory}/${project.build.finalName}.jar</source>
      <outputDirectory>/</outputDirectory>
    </file>
  </files>

  <fileSets>
    <!-- Include runtime dependencies copied by dependency:copy-dependencies -->
    <fileSet>
      <directory>${project.build.directory}/lib</directory>
      <outputDirectory>lib</outputDirectory>
      <includes>
        <include>**/*.jar</include>
      </includes>
      <useDefaultExcludes>true</useDefaultExcludes>
      <filtered>false</filtered>
      <fileMode>0644</fileMode>
    </fileSet>

    <!-- Include jcef-bundle folder if present in the project (natives/resources) -->
    <fileSet>
      <directory>${project.basedir}/jcef-bundle</directory>
      <outputDirectory>jcef-bundle</outputDirectory>
      <useDefaultExcludes>true</useDefaultExcludes>
      <filtered>false</filtered>
    </fileSet>
  </fileSets>
</assembly>
